export async function onRequestGet(c){if(!c.env.TOYSKUB_MEDIA)return new Response('Media storage unavailable',{status:503});const raw=c.params.path;const key=Array.isArray(raw)?raw.join('/'):String(raw||'');const obj=await c.env.TOYSKUB_MEDIA.get(key);if(!obj)return new Response('Not found',{status:404});const headers=new Headers();obj.writeHttpMetadata(headers);headers.set('etag',obj.httpEtag);headers.set('cache-control',headers.get('cache-control')||'public, max-age=31536000, immutable');return new Response(obj.body,{headers})}
